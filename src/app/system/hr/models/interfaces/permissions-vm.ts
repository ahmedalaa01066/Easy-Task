export interface CreatePermissionViewModel {
    id: string;
    name: string;
    maxHours: number;
    minHours: number;
    maxRepeatTimes: number;
    maxHoursPerMonth: number;
}
export interface SearchPermissionViewModel {
    Name: string;
}