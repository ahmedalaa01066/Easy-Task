export interface LoginViewModel {
    email: string;
    password: string;
}
export interface UserViewModel {
    id: string;
    name: string;
    phone: string;
    email: string;
    path?: string
}